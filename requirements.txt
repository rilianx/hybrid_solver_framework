pulp>=3.3,<4   # 4.0 elimina LpVariable(...) directo y PULP_CBC_CMD, que usan los problem_model.py
pytest>=8.0
# clientes LLM (opcionales; solo para llm.client.OpenAIClient / AnthropicClient)
openai>=1.50
anthropic>=0.40
optuna>=3.6
